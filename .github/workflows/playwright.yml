name: Playwright Tests
on:
  push:
    branches: [ main, master, pom_structure ]
  pull_request:
    branches: [ main, master, pom_structure ]
jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
    # -----------------------------
    # Checkout Code
    # -----------------------------
    - uses: actions/checkout@v5

    # -----------------------------
    # Setup Python
    # -----------------------------
    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.13'

    # -----------------------------
    # Install Dependencies
    # -----------------------------
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    # -----------------------------
    # Install Playwright Browsers
    # -----------------------------
    - name: Ensure browsers are installed
      run: python -m playwright install --with-deps

    # -----------------------------
    # Run Tests
    # -----------------------------
    - name: Run your tests
      run: | 
        pytest tests/UI/test_loginPage.py

    # -----------------------------
    # Upload HTML Report
    # -----------------------------
    - name: Upload HTML Test Report
      uses: actions/upload-artifact@v4
      if: ${{ !cancelled() }}
      with:
        name: html-report
        path: reports/reports.html

    # -----------------------------
    #   Upload Raw Allure Results
    # -----------------------------
    - name: Upload Allure Results
      uses: actions/upload-artifact@v4
      if: ${{ !cancelled() }}
      with:
        name: allure-results
        path: reports/allure_reports/
        
    # -----------------------------
    # Generate Allure Static Report
    # -----------------------------
    # - name: Install Allure CLI
    #   run: |
    #     set -e

    #     ALLURE_TAG=$(curl -s https://api.github.com/repos/allure-framework/allure2/releases/latest \
    #       | grep -E '"tag_name":' | head -n1 | cut -d '"' -f 4)
    #     echo "Latest Allure CLI tag: $ALLURE_TAG"

    #     ZIP_NAME="allure-commandline-${ALLURE_TAG}.zip"
    #     ZIP_URL="https://github.com/allure-framework/allure2/releases/download/${ALLURE_TAG}/${ZIP_NAME}"
    #     echo "Trying download: $ZIP_URL"

    #     if wget --spider "$ZIP_URL"; then
    #       echo "Download exists — using $ALLURE_TAG"
    #     else
    #       echo "Download failed — fallback to 2.35.1"
    #       ALLURE_TAG="2.35.1"
    #       ZIP_NAME="allure-commandline-${ALLURE_TAG}.zip"
    #       ZIP_URL="https://github.com/allure-framework/allure2/releases/download/${ALLURE_TAG}/${ZIP_NAME}"
    #     fi

    #     wget "$ZIP_URL" -O allure.zip
    #     sudo unzip allure.zip -d /opt/allure
    #     sudo ln -s /opt/allure/allure-${ALLURE_TAG}/bin/allure /usr/local/bin/allure

    #     allure --version

    # -----------------------------
    # Generate Allure HTML Report
    # -----------------------------
    # - name: Generate Allure HTML Report
    #   run: |
    #     allure generate reports/allure_reports -o reports/allure_html --clean
        
    # -----------------------------
    # Upload Final Allure HTML Report
    # -----------------------------
    # - name: Upload Allure HTML Report
    #   uses: actions/upload-artifact@v4
    #   if: ${{ !cancelled() }}
    #   with:
    #     name: allure-html-report
    #     path: reports/allure_html/    